name: Discord PR Notification

on:
  pull_request:
    types: [opened, reopened]

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: https://discord.com/api/webhooks/1417631604423266425/pDfUyIl1B5CK-BO5Nc8yk8E-j1dOiftleR3EQkuDnwrMxyyvZSRIcjLpKyXGLvrOAlg0
        with:
          args: |
            🔔 **New Pull Request Opened** by ${{ github.event.pull_request.user.login }}
            
            **Details:**
            ${{ github.event.pull_request.title }}
            ${{ github.event.pull_request.html_url }}